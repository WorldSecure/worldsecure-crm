# מדריך התקנה מהיר - מערכת CRM לניהול מחסן

## ⚡ התקנה מהירה (5 דקות)

### 1️⃣ הורד והתקן Node.js
- גש ל: https://nodejs.org/
- הורד את הגרסה LTS (מומלץ)
- התקן עם האופציות ברירת המחדל
- ✅ סמן "Add to PATH"

### 2️⃣ חלץ את הקבצים
- חלץ את התיקייה `crm-project` ל:
  ```
  C:\Users\amit\crm-project
  ```

### 3️⃣ התקן תלויות (פעם אחת בלבד)

פתח **Command Prompt** או **PowerShell** כמנהל (Run as Administrator)

```bash
# התקן Backend
cd C:\Users\amit\crm-project\backend
npm install

# התקן Frontend  
cd C:\Users\amit\crm-project\frontend
npm install
```

זה ייקח 2-3 דקות...

---

## 🚀 הפעלת המערכת

### אופציה א': הפעלה אוטומטית (מומלץ!)

פשוט לחץ פעמיים על הקובץ:
```
C:\Users\amit\crm-project\start-crm.bat
```

המערכת תיפתח אוטומטית בדפדפן! 🎉

### אופציה ב': הפעלה ידנית

**חלון 1 - Backend:**
```bash
cd C:\Users\amit\crm-project\backend
npm start
```

**חלון 2 - Frontend:**
```bash
cd C:\Users\amit\crm-project\frontend
npm start
```

---

## 🌐 גישה למערכת

### מהמחשב המקומי:
```
http://localhost:3000
```

### ממחשבים אחרים ברשת:

1. **מצא את ה-IP שלך:**
   ```bash
   ipconfig
   ```
   חפש את `IPv4 Address` (לדוגמה: `192.168.1.100`)

2. **במחשב אחר ברשת, פתח:**
   ```
   http://192.168.1.100:3000
   ```

---

## 👤 יצירת משתמש ראשון

1. פתח את המערכת בדפדפן
2. לחץ על "הרשם"
3. מלא פרטים:
   - שם משתמש
   - אימייל
   - סיסמה (לפחות 6 תווים)
4. לחץ "הרשם"
5. התחבר עם הפרטים שיצרת

---

## 📋 צעדים ראשונים במערכת

אחרי ההתחברות:

1. **הגדרות חברה** ⚙️
   - העלה לוגו
   - מלא פרטי חברה
   
2. **הוסף קטגוריות** 📂
   - אלקטרוניקה, ריהוט, ציוד משרד וכו'

3. **הוסף מוצרים** 📦
   - מק"ט, שם, תיאור
   - קטגוריה, מחיר
   - כמות במלאי, מלאי מינימום

4. **הוסף ספקים** 🏭
   - שם, כתובת, טלפון
   - אימייל, עוסק מורשה

5. **הוסף לקוחות** 👥
   - שם, כתובת, טלפון
   - אימייל, עוסק מורשה

---

## 🔧 פתרון בעיות נפוצות

### ❌ "npm is not recognized"
**פתרון:** התקן Node.js מחדש עם האופציה "Add to PATH"

### ❌ "Port 3000 is already in use"
**פתרון:** סגור תוכניות אחרות שמשתמשות בפורט 3000, או:
```bash
# Windows
netstat -ano | findstr :3000
taskkill /PID <מספר_תהליך> /F
```

### ❌ לא רואה את המערכת ממחשב אחר
**פתרון:**
1. וודא שהמחשבים באותה רשת
2. כבה זמנית את ה-Firewall ובדוק
3. השתמש בכתובת IP הנכונה (לא 127.0.0.1)

### ❌ בעיות התקנת npm
**פתרון:**
```bash
npm cache clean --force
npm install
```

---

## 💾 גיבוי

**חשוב!** גבה את הקובץ הזה באופן קבוע:
```
C:\Users\amit\crm-project\backend\warehouse.db
```

זה בסיס הנתונים שמכיל את כל המידע!

---

## 🎯 עצות לשימוש

1. **גיבוי יומי** - הקובץ warehouse.db הוא הכל!
2. **סיסמאות חזקות** - לכל המשתמשים
3. **עדכוני מלאי** - עדכן אחרי כל קליטה/הוצאה
4. **מלאי מינימום** - הגדר לכל מוצר להתראות
5. **פרטי ספקים/לקוחות** - מלא מלא להפקת מסמכים

---

## 📞 תמיכה

- בעיות טכניות? פנה למפתח המערכת
- רעיונות לשיפור? אנחנו מקשיבים!

---

**בהצלחה! 🎉**

המערכת שלך מוכנה לשימוש! 
